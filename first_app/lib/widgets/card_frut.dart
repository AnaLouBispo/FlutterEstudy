import 'package:flutter/material.dart';

class CardFrut extends StatelessWidget {
  const CardFrut({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}